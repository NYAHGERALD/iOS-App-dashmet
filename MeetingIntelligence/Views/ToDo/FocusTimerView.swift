import SwiftUI

// Placeholder - Focus Timer removed in favor of backend-synced tasks
struct FocusTimerView: View {
    let colorScheme: ColorScheme
    var body: some View {
        Text("Focus Timer")
    }
}
